The app will automatically download the ngrok binary at first run.
You do NOT need to put anything here manually.

If auto-download fails (no internet at first launch),
you can manually place the ngrok ARM64 binary here named "ngrok".

Download from: https://ngrok.com/download
Choose: Linux → ARM64
Extract the "ngrok" binary and place it in this assets folder.
